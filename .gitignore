.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.hypothesis/
build/
*.so
*.dylib
.DS_Store
# large raw datasets are downloaded on demand (see data/README.md)
data/raw/
# experiment outputs are reproducible from the CLI (provenance.json records seed + git commit);
# full-size results are committed, smoke-test ("quick") outputs are not
results/**/quick/
